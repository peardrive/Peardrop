node_modules/
.expo/
dist/
*.log
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
web-build/
android/.gradle/
android/app/build/
android/build/
ios/Pods/
ios/build/
.DS_Store

# Generated native projects (Continuous Native Generation) — recreate with
# 'npx expo prebuild'. Release signing intentionally uses the debug keystore
# (sideload/testing phase), so there is nothing secret in here either.
/android/
/ios/
